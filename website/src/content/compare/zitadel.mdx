---
title: FoxAuth compared with Zitadel
competitor: Zitadel
description: Zitadel is a multi-tenant identity platform with SAML, SCIM and a managed cloud. FoxAuth is a smaller OAuth 2.1 server with the banking-grade profiles built in.
lastChecked: '2026-09-06'
sources:
  - https://github.com/zitadel/zitadel
  - https://zitadel.com/self-hosted
  - https://zitadel.com/docs
bottomLine: 'Pick Zitadel if you need SAML, SCIM provisioning or a managed cloud with an enterprise contract behind it. Pick FoxAuth if you want OAuth 2.1 behaviour by default, the banking-grade profiles without a plan tier, and a licence that is not AGPL.'
chooseThem:
  - 'You need SAML 2.0 or SCIM 2.0 provisioning as well as OpenID Connect.'
  - 'You want a managed cloud option and an enterprise support contract from the vendor.'
  - 'You want a mature multi-tenant model with organisations built into the product.'
  - 'AGPL-3.0 is acceptable to you, or you will buy the commercial licence that replaces it.'
chooseUs:
  - 'AGPL-3.0 is a problem for your distribution model and you want a licence that converts to Apache 2.0.'
  - 'You need DPoP, PAR, CIBA, mTLS, RAR or JARM, none of which Zitadel documents.'
  - 'Your team writes TypeScript and would rather extend named seams than run a Go service.'
  - 'You want an AI agent administering the instance through the same audited path a human uses.'
faq:
  - question: 'Does FoxAuth support SAML like Zitadel does?'
    answer: "No. FoxAuth implements OAuth 2.1 and OpenID Connect only, and there is no SAML support planned. Zitadel documents SAML 2.0 and SCIM 2.0 alongside OIDC. If any application in your estate can only speak SAML, that settles it in Zitadel's favour and no other row on this page matters."
  - question: 'How do the licences differ?'
    answer: "Zitadel is AGPL-3.0 with Apache 2.0 and MIT exceptions for specific directories, and sells an enterprise contract whose commercial licence replaces the AGPL. FoxAuth is FSL-1.1-ALv2: you may read, modify, self-host and redistribute it and build a business around it, but not offer it as a competing hosted service — and each version converts to Apache 2.0 two years after it ships. AGPL's network-copyleft and FSL's competing-service restriction rule out different things, so which is the problem depends on what you are building."
  - question: 'Can I migrate from Zitadel to FoxAuth?'
    answer: "OIDC clients port over with an issuer and client id change. What does not port is anything relying on Zitadel's SAML or SCIM support, its organisation model, or its management API — FoxAuth models tenancy as projects and user buckets and exposes a different admin API. Assess the migration by how much of your estate is OIDC-only, because that part is straightforward and the rest is not."
  - question: 'Which is more production-ready?'
    answer: "Zitadel, on deployment history and on having a vendor-operated cloud and enterprise contracts behind it. FoxAuth is at a 0.2.0 release, so its HTTP surface and admin API may still change between minor versions. FoxAuth's advantage is in specific protocol coverage and licence terms, not in maturity, and this page will not pretend otherwise."
groups:
  - label: Scope and stack
    rows:
      - dimension: Protocols beyond OAuth and OIDC
        us:
          {
            status: no,
            text: 'OAuth 2.1 and OpenID Connect only. No SAML, and none planned.'
          }
        them: { status: yes, text: 'SAML 2.0 and SCIM 2.0 alongside OIDC.' }
        verdict: them
        why: 'A single SAML-only application in your estate decides this comparison on its own.'
        note: 'The repository lists "SAML 2.0" and "SCIM 2.0 Server" among its features.'
      - dimension: Runtime and stack
        us:
          { status: yes, text: 'TypeScript on Bun; the HTTP layer is Elysia.' }
        them: { status: yes, text: 'Go.' }
        verdict: different
        why: 'Pick the one your team can read and debug under pressure.'
      - dimension: Storage
        us:
          {
            status: yes,
            text: 'MongoDB in production, in-memory for tests, behind one adapter interface.'
          }
        them: { status: yes, text: 'PostgreSQL 14 or newer.' }
        verdict: them
        why: 'Most teams already run PostgreSQL; fewer already run MongoDB.'
        note: 'The repository states "PostgreSQL (≥ 14)".'
      - dimension: Multi-tenancy
        us:
          {
            status: yes,
            text: 'Projects and user buckets, with group ownership deciding who administers what.'
          }
        them:
          {
            status: yes,
            text: 'Organisations, presented as a core part of the product.'
          }
        verdict: them
        why: 'Zitadel has built its product around multi-tenancy for longer.'
  - label: Protocol coverage
    rows:
      - dimension: Core OAuth and OIDC
        us:
          {
            status: yes,
            text: 'OAuth 2.1 with PKCE mandatory for every client, OIDC Core and Discovery.'
          }
        them: { status: yes, text: 'OpenID Connect certified.' }
        verdict: them
        why: 'Certification is third-party evidence; FoxAuth has none yet.'
      - dimension: Device flow
        us: { status: yes, text: 'RFC 8628, behind a feature flag.' }
        them:
          {
            status: yes,
            text: 'Device authorization, listed as a product feature.'
          }
        verdict: even
        why: 'Both implement it.'
      - dimension: Sender-constrained tokens
        us: { status: yes, text: 'DPoP (RFC 9449) and mTLS (RFC 8705).' }
        them: { status: unknown, text: 'Not documented.' }
        verdict: us
        why: 'If a regulator asks for sender-constrained tokens, this row is the answer.'
      - dimension: 'PAR, CIBA, RAR and JARM'
        us:
          {
            status: yes,
            text: 'PAR (RFC 9126), CIBA, RAR (RFC 9396) and JARM, each behind a flag.'
          }
        them: { status: unknown, text: 'Not documented.' }
        verdict: us
        why: 'The banking-grade profiles are implemented here rather than on a roadmap.'
      - dimension: Machine-to-machine
        us:
          {
            status: yes,
            text: 'Client credentials, with JWT client authentication.'
          }
        them:
          {
            status: yes,
            text: 'JWT Profile, personal access tokens and client credentials.'
          }
        verdict: them
        why: 'Zitadel additionally offers personal access tokens, which FoxAuth has no equivalent of.'
  - label: Administration and licensing
    rows:
      - dimension: Administration by an AI agent
        us:
          {
            status: yes,
            text: "MCP control plane over the console's own routes, audited and confirmation-gated. Off by default."
          }
        them: { status: unknown, text: 'Not documented.' }
        verdict: us
        why: "The agent path here is the console's code rather than a second privileged API."
      - dimension: Managed hosting
        us:
          {
            status: flag,
            text: 'A managed instance is on a waitlist; self-hosting is the only option today.'
          }
        them:
          {
            status: yes,
            text: 'A vendor-operated cloud, alongside self-hosting.'
          }
        verdict: them
        why: 'If you would rather not run it at all, Zitadel can take it off you today.'
      - dimension: Licence
        us:
          {
            status: flag,
            text: 'FSL-1.1-ALv2: source-available, converting to Apache 2.0 after two years.'
          }
        them:
          {
            status: flag,
            text: 'AGPL-3.0, with Apache 2.0 and MIT exceptions for specific directories.'
          }
        verdict: different
        why: "Neither is permissive. AGPL's network copyleft and FSL's competing-service restriction rule out different things."
        note: 'Zitadel sells an enterprise contract whose commercial licence replaces the AGPL.'
      - dimension: Maturity
        us:
          {
            status: flag,
            text: '0.2.0. The HTTP surface and admin API may still change between minor releases.'
          }
        them:
          {
            status: yes,
            text: 'Years of production deployment, a vendor cloud and enterprise contracts.'
          }
        verdict: them
        why: 'The maturity gap is real and is the strongest argument for Zitadel.'
---

## The row that usually decides it

If anything in your estate speaks SAML and only SAML, this comparison is over: Zitadel documents
SAML 2.0 and SCIM 2.0, FoxAuth implements neither and has no plans to. That is not a gap waiting to
be filled — it is a deliberate scope boundary, and it is the honest reason to choose Zitadel.

## Where FoxAuth is genuinely ahead

Two places, and both are narrow.

The first is the banking-grade profiles. DPoP, PAR, CIBA, mTLS, RAR and JARM are implemented and
flag-controlled on the instance you run. Zitadel's documentation does not describe them, which this
page records as "not documented" rather than "absent" — the distinction matters, and a claim their
own documentation does not make is not one we will make for them.

The second is the licence, and only for some readers. AGPL-3.0 is a genuine obstacle if you
distribute software or offer a network service built on it, and the enterprise contract that
replaces it is a commercial conversation. FSL-1.1-ALv2 has a narrower restriction — you may do
anything except offer FoxAuth as a competing hosted service — and it expires on a schedule, with each
version converting to Apache 2.0 two years after it ships.

Neither licence is permissive. If that is what you need, neither of these is your answer.
