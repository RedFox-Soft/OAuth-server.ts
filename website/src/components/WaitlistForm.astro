---
import SubmitButton from './ui/SubmitButton.astro';

/*
 * Formspree when the form id is configured, a mailto link otherwise. The fallback matters: the site
 * builds and deploys from a repository that carries no secrets, so an unconfigured build must still
 * give a reader a way to reach us.
 */
const formId = import.meta.env.PUBLIC_FORMSPREE_WAITLIST;
const subject = 'FoxAuth Cloud waitlist';
const fieldClass =
	'w-full rounded-control border border-gray-300 bg-white px-3 py-2 text-sm text-gray-900 placeholder:text-gray-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-500 dark:border-gray-700 dark:bg-gray-950 dark:text-gray-100';
---

{
	formId ? (
		<form method="POST" action={`https://formspree.io/f/${formId}`} class="flex flex-col gap-3">
			<input type="hidden" name="_subject" value={subject} />
			<input type="text" name="_gotcha" tabindex="-1" autocomplete="off" class="hidden" />
			<label class="flex flex-col gap-1 text-sm font-medium">
				Work email
				<input
					type="email"
					name="email"
					required
					autocomplete="email"
					placeholder="you@company.com"
					class={fieldClass}
				/>
			</label>
			<SubmitButton>Join the waitlist</SubmitButton>
			<p class="text-xs text-gray-600 dark:text-gray-400">
				One email when the cloud opens. Nothing else, and no commitment.
			</p>
		</form>
	) : (
		<p class="text-sm text-gray-600 dark:text-gray-400">
			Email us at{' '}
			<a
				href={`mailto:hello@foxauth.dev?subject=${encodeURIComponent(subject)}`}
				class="font-medium text-accent-700 dark:text-accent-200"
			>
				hello@foxauth.dev
			</a>{' '}
			to join the waitlist. One email when the cloud opens, and no commitment.
		</p>
	)
}
