[test]
preload = ["./test/preload.ts"]
coverageSkipTestFiles = true
# Only entry left: test/helpers/attention.spec.ts uses Mocha's `context()` and has no bun:test
# equivalent yet — it is migrated by backlog task 24. The former test/cors/* entries are gone: that
# suite is rewritten against the delivered CORS contract and runs.
pathIgnorePatterns = ["test/helpers/attention.spec.ts"]
