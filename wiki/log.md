# Wiki Log

Append-only chronological record of operations on the wiki. Each entry begins with `## [YYYY-MM-DD] <op> | <description>` so it's parseable with `grep "^## \[" log.md | tail -N`.

Operations:
- `ingest` — a source was processed into the wiki.
- `query` — a question was answered against the wiki (typically only logged when the answer was filed back as synthesis).
- `lint` — a health check was run.
- `schema` — the schema was modified.
- `shard` — an index was sharded.

---

## [2026-07-31] schema | Fixed script invocation (wiki/bin/wiki.py launcher), recorded codebase-as-source and transient-artifact rules, added `subsystem` node type and `implements` predicate to the ontology.

## [2026-07-31] ingest | OAuth server codebase (lib/) at commit 2125ad0 — first real ingest. Pilot pass: 1 source page + 5 pages on durable subsystem contracts (token payload access, client identity, account resolution, feature-flag gating, eventBus).

## [2026-08-27] ingest | Admin console sign-out fix: new page on cookie Path identity (`remove()` omits Path); `admin-console-signin` gains the sign-out half — two sessions, and why not RP-initiated logout.

## [2026-08-27] ingest | TOTP second factor (spec 027): new concept page on the bucket-level second factor — why the algorithm is in-repo (published RFC vectors beat a dependency), why the bucket field is a boolean beside `passwordLogin` rather than an enum replacing it, and the four state placements that buy deletion integrity, expiry, un-guessable enrolment and a cross-interaction throttle. Records two incidental findings: `Interaction.persist()` has never been able to succeed, and the Mongo user store's `$set`-only patch could not clear a field.

## [2026-08-27] update | Admin bucket second factor: `totp-second-factor` gains the administrator-bucket section — why the reserved bucket needed its own endpoint (`assertNotReserved` refuses it on the generic routes and its 403 names `/admin/api/admins`), and why that endpoint carries one field. Records the `emailVerificationRequired` console brick: both admin-creation paths write `verified: false` and no verification mail is ever sent for this bucket, so the flag would refuse every administrator with no way back.

## [2026-08-29] ingest | Group ownership (spec 033): new concept page on the ownership model that replaced `managedBy: string[]` — a group owns every project and bucket and belonging to it is the only thing that grants access, with a personal group per account so ownership has a single mechanism and sharing personal work is adding a member rather than a transfer. Records why owner-vs-member is a property of the membership and not a role on the account, why the active scope is server-held and re-validated per request, and the migration rule that groups by *identical* manager set — `{A,B}` and `{A,C}` must not merge, which would be a cross-tenant leak created by the migration itself. Three incidental findings: a foreign container answered 403 while a nonexistent one answered 404, handing out an existence oracle over every id; the permanent-owner rule for a personal group was never enforced, because the last-owner check does not fire once a second owner exists; and bucket listing disagreed with bucket access, so a bucket you could administer never appeared. Updates `admin-audit-trail` (the read is group-scoped, not super-admin only), `admin-mcp-control-plane` (`group_delete` is the third withholding, invitation acceptance the third inapplicable) and `admin-console-signin` (the session carries the active scope).

## [2026-09-01] graph | Four subsystem entity pages written to resolve dangling typed edges: [[html-rendering]], [[elysia-lifecycle]], [[addon-registry]] and [[admin-console]] were referenced by `depends_on` edges on five concept pages but had never been created, so the graph carried five edges pointing at nodes that did not resolve. Each page is grounded in the code it documents — the single-`htmlResponse` guarantee and why it is deliberately not a lifecycle plugin; the callback-shaped hooks whose mount order in `lib/index.ts` is a correctness constraint rather than an optimisation; the call-time `resolve()` seam whose type-only imports keep the model graph out of the test preload; and the `routes.ts`/`index.ts` split that gives the MCP plane one definition of the admin API. Ontology gains two predicates, `complements` and `constrained_by`, which two existing evidence-backed edges already used without declaring.

## [2026-09-01] lint | Cleared the two structural findings the graph round left behind. The dangling `[[bun-beforeeach-describe-scope]]` link on `per-origin-rate-limiting` is gone rather than filled in: the claim it pointed at — that a top-level `beforeEach` does not run for `describe`-nested tests — was checked against bun 1.4.0 and is false, in both declaration orders. The real hazard is the one that page already describes, process-wide module state, so the section now names the second instance of it: `test/preload.ts` seeds `jwksStore` with three signing keys once and never resets it, so a deleted key changes what every later spec *file* sees. `login-door-throttle` was an orphan and now has three inbound links — from [[totp-second-factor]] (whose code-step throttle it is, moved one door earlier), from [[self-service-password-reset]] (the escape hatch from a shut door), and from [[per-origin-rate-limiting]], whose closing paragraph still called that work pending; it has since shipped, and the per-identity throttle is what survives the origin rotation the per-origin limiter deliberately fails open under.

## [2026-09-03] lint | Full pass — structural, graph and semantic — after two ingests. Both scripts now report clean. Structural found one orphan and the graph eight typed-orphan nodes: [[sentry-plugin-not-used]] had no inbound link, not even from [[two-meanings-of-origin]], which is about the very event it explains hand-building; and eight pages carried a `graph:` block with no `relationships:` at all — among them [[admin-audit-trail]], the second-most-linked page in the wiki, nineteen inbound prose links and zero typed edges. Ten evidence-backed edges closed all eight, and the graph recompiled to 287. Three pages had `updated:` behind the commit that last changed their content; the eleven other date mismatches were single-line backlink additions and were left alone, since the practice here is to bump for content and not for an inbound link. Two findings neither script checks, both left for a decision: three tags in use (`security`, `testing`, `admin`) are undeclared in SCHEMA.md's taxonomy, all five uses arriving with the last two ingests, and [[sentry-plugin-not-used]] names a `sources:` entry, `sentry-elysia-10.73.0`, that has no source page. No contradictions — the four pages whose subject a later commit touched already matched the code.

## [2026-09-03] ingest | Two pages for shipped work that had no wiki home. [[settings-console-descriptor]]: the settings console holds no second list of settings — panes, summaries, units and risk marks are descriptor fields served from `lib/admin/settings/catalog.ts`, because a copy in the browser bundle could name a pane the catalog never fills or miss one it does, with nothing to catch either. It also records the demotion criterion written at `lib/sentry/dispatch.ts:31` ("is there a question an operator could answer to set it") and the audit that applied it to every catalogued setting: exactly one demotion, `dpop.allowReplay`, which removes a check in every direction so no operator question stands behind it, and two near-misses that look demotable and are not — the `rateLimit.*` keys pinned by a test written to prevent it, and `loginThrottle.windowCeilingSeconds`, which alone sets the settled rate of a sustained attack once the doubling clamps. [[user-code-mask-entropy]]: why a `deviceFlow.mask` with no asterisk was a security defect and not a cosmetic one — the asterisk count is the whole entropy of a device code, so `generate` returned the mask verbatim, identical for every device that ever paired, and the character check that admitted it never asked about shape. Both pages keep the reasoning for the scoping calls: the mask check sits behind the enable flag yet leaves no hole, because the administrative PUT validates the merged configuration, and a bespoke editor is pinned by test to `validateConfiguration` in the one direction that matters.

## [2026-09-03] lint | Cleared the two findings the pass had left for a decision, both of them about frontmatter neither script reads. The dangling `sources:` reference now has a page: [[sentry-elysia-10.73.0]], a source read but never installed — `@sentry/bun` is the dependency and two assertions in `test/sentry/no_instrumentation.spec.ts` pin the plugin's absence — so it pins a *version* where the codebase source pins a revision, and `raw:` is null because there is no vendored tree to cite. The extractor had been dropping that reference silently, emitting 41 `sourced_from` edges for 42 declared, which is how a missing source page hides. SCHEMA.md gains the external-source rule that follows from it. The three undeclared tags are **removed rather than declared**: `security` had reached three of the roughly fifteen pages it fits on an authorization server, and a tag carried by a minority of the pages it fits is worse than none, because a filtered query comes back quietly incomplete instead of empty; `testing` and `admin` had one use each in areas full of untagged pages of the same kind. Four pages take `contract` in its place, where the page's subject really is an invariant. SCHEMA.md records the rule so a later ingest does not re-add them: a tag earns a place only if it fits every page it is applied to and does not fit most of the wiki.

## [2026-09-07] ingest | [[security-assurance-evidence]] — the work that turned SECURITY.md from a policy into a policy with proof: a threat model page and an assurance page under `docs/security/`, a `Security` workflow (CodeQL over TypeScript and the workflows themselves, `bun audit --audit-level=high` on the server and the site lockfiles, dependency review, Trivy image scan to code scanning), a `Scorecard` workflow and a Dependabot config. The page keeps the reasoning behind four calls: the Known limitations section is the evidence rather than a confession; the image scan publishes but does not fail the build while the dependency audit does, because one remedy is inside the repository and the other is not; the website's lockfile is audited too; and actions stay on major tags for now, with Scorecard's pinning check expected to say so. `bun audit fix` moved picomatch 4.0.3 to 4.0.4 so the gate was green from its first run. No tests, per the owner's 2026-09-03 decision on site and repo files.

## [2026-09-08] ingest | [[mcp-server-authorization]] — the feature that makes the server an authorization server *for* MCP servers rather than only for its own admin plane, and the reasoning that is not recoverable from the diff. Two blockers, both fixed by turning code into data: `getResourceServerInfo` threw for every audience but `${ISSUER}/mcp`, and a client belonging to no project fell to a hard-coded bucket. Rule 3 of bucket resolution answers a question the design had left open, safe because an administrator authored the resource — the parameter selects among an operator's options and cannot create one. The page keeps six calls that cost something to rediscover: the branch in `tryFindClient` must sit AFTER the adapter read or it shadows `test/client_id_uri/`; dot segments must be checked on the raw input because parsing resolves them away, `%2e%2e` included; an expiry index on the `Client` area is refused by the inventory itself, so reclamation is an explicit sweep behind a new adapter method; restricting a permitted redirect target is what the CIMD draft names but drags in a SHOULD to run a public metadata-document service; the loopback acknowledgement is one exported string shared by the route and the console; and `findAccount` must be passed the resource, which it was not at first — the symptom was a 500 in the consent prompt, not a refusal. Also records where the specification deliberately does not decide: token format is a product choice, and scope accumulation is the client's job.

## [2026-09-10] ingest | [[postgresql-backend]] — the second production datastore, and the four calls that cost something to get wrong first. Two of them are expiry designs reversed by reading call sites instead of reasoning from how storage ought to behave: filtering expired rows on read would have *created* a divergence, because `MongoAdapter.find` does not filter either and expiry is enforced a layer up; and clearing a stale `expiresAt` on an untimed upsert — proposed as a convergence, inside the backend the feature promised not to touch — would have turned a stale expiry into an immortal record had any path ever hit it, for a case the call sites prove unreachable. Replaced by an invariant and a hermetic guard. The third is the defect a tolerant decoder hid for a whole commit: pre-stringified jsonb stores a jsonb *string*, every predicate inside the document silently matches nothing, and a decoder that parses it back makes every round trip look perfect; the decoder now throws. The fourth is the probe split — the lazily connecting handle that keeps the module graph clean is also what would let the server serve against a dead database, so `/ready` joins `/health` rather than `/health` learning to touch storage. Also records why the fidelity tier is scripts rather than specs (Principle III's three conditions, and `bun test` must not be able to reach them) and why the divergence register is a gate rather than a document.

## [2026-09-10] ingest | [[test-admission-rule]] — constitution 3.0.0 rewrites Principle V: a test proves a user case or a security invariant, and one proving neither is removed. The old principle required integration coverage and then permitted unit tests without bounding them, which is how the suite reached 285 spec files, ~2,835 cases and 75,020 lines with 263 of 285 specs importing `lib/` internals against 80 exercising the HTTP layer. The page keeps three things the diff cannot carry, each of which would have produced a worse rule if taken at face value. The drift and parity guards were never structure tests: they enumerate the *mounted route table of the constructed application* (`elysia.routes`, a freshly mounted `adminApiRoutes`) rather than source text, so renaming every handler leaves them green while mounting one unaudited route fails them — and what they close is a claim about absence that no example-based test can reach, which both files already said in the same words. There are two sentence templates rather than one because a completeness guard has no trigger, and forcing behavioural prose onto it would have deleted exactly the tests worth keeping; no third category was created, since a guard is a user case with the operator as audience and a security invariant with the attacker as audience. And a change-detector can sit *inside* a real guard: the two-way parity assertion in `catalogue_drift` is admissible while `expect(mcpCatalogue.length).toBe(65)` two lines above it is not, which is what forced classification down to the individual test case. Also records why the naming rule does not mandate the word "should" (2,281 of 2,835 names already open with an assertive verb, 20 use "should"; the real gap is the condition slot at 35%), and the first consequence applied: `test/helpers/attention.spec.ts` deleted rather than migrated, closing issue #17 — it asserted a log prefix, passing the articulation half of the admission test and failing the audience half, and had never run at all.

## [2026-09-11] reconcile | [[mongodb-test-fidelity]] — Tier 1 was never built, and the page said it was. The "storage encoding contract" tier it describes in the present tense — BSON round trips in `test/storage_contract/`, no server, always runs — does not exist: `grep -r BSON test` matches nothing at this commit or at `f6eb1b9`, and the two specs for those secrets import the memory implementation. Found by replaying the original defect as an SC-007 probe: removing the `Binary` unwrap leaves that directory at 294 pass / 0 fail and the full suite green, so the class of bug the page was written about is still uncovered by the default suite. Recorded as G-008. The general hazard is now stated on the page itself: a decision page reads, months later, exactly like a description of shipped work, and the reader who most needs the distinction is the one deciding whether a defect class is already covered.

## [2026-09-11] revise | [[test-admission-rule]] — the rule deleted its own enforcement mechanism. FR-026 specified and Story 4 shipped a guard requiring a `@proves` declaration in every spec file; applying Principle V to it removed it. The principle opens "every test in the suite", the guard was in the suite, and it fails the rule's own second question: none of the four audiences depends on whether a spec file declares its intent, so it is structurally the console-notice prefix the rule deletes. The distinction from the admissible guards is not a technicality — the audit route guard enumerates the mounted route table of the constructed application and its failure means an administrative mutation goes unrecorded, while this one enumerated files on disk. It also exhibited its category's failure mode twice: it could assert only that a declaration was present and over twenty characters, and a page of source code satisfies both, which is what happened when its pattern matched the tag inside its own regular expression. Enforcement is now review against `test/RULES.md`. General form: a rule about what tests are for cannot be held by a test, because the holder is subject to the rule.

## [2026-09-11] ingest | [[unknown-request-parameters]] — conformance reported `oidcc-ensure-request-with-unknown-parameter-succeeds` and 28 FAPI2 modules failing on `Property 'client_assertion' should not be provided`; the cause is that `normalize: false` plus closed TypeBox objects makes every undeclared parameter a 422. Reading the specs rather than the one reported endpoint widened it from one site to six: RFC 6749 carries the identical MUST in §3.1 *and* §3.2, so `/token` is in scope despite an earlier decision to keep it strict — a decision the fix preserves, since the schema stays strict and standard and only stops erroring. Two things the diff cannot carry. First, once absence from a schema means "ignore", four deliberate refusals implemented by omission silently became acceptances — `request_uri` at `/par` (RFC 9126 §2.1 step 2), `request`/`request_uri` inside a Request Object (OIDC Core §6.1 inception), `authorization_details` at `/token` (RFC 9396 §7, where ignoring hands back a broader token than the client asked to narrow to); the suite caught all of them, which is the strongest evidence for that half of the boundary. Second, `normalize: true` was measured rather than argued: 14 failures against 10, and it strips `x-client-cert` — read by `lib/addon/mtls.ts` through no schema — so a certificate-bound token presented *with* its certificate answers 401, and it cannot reach the Request Object at all because that validation is a direct `getSchemaValidator().Check()` outside the route pipeline. Also records a test-design trap found by reintroducing the defect: comparing status alone passes on `/auth` in both states, because a refusal there is delivered as a redirect and is a 303 like the success.

## [2026-09-13] ingest | [[authentication-context-reporting]] — two `acr` entries in `CONFORMANCE.md` closed at `4101b93`, and the reported diagnosis was wrong in the half that mattered. It said nothing assigned an ACR and everything downstream was wired; the second clause was wrong by one hop, because `OIDCContext` read `session.acr` while the value lives on the payload, so the context was unreachable even when a session carried one — the second recorded instance of [[token-payload-access-contract]] and the more expensive one, since it sat inside an accessor where every call site reads correctly and the repair that looks complete is inert. Three things the diff cannot carry. The obligation is conditional on a flag that ships off: §5.5.1.1 says "and the implementation supports the claims parameter", so with `claimsParameter.enabled` false the loop is unreachable and the suite can only warn — obligation and defect appear and disappear together. The backchannel breaks the same invariant from the opposite end, producing not a loop but a token that should not exist, which is why the refusal is recorded in `backchannelResult` at the moment the outcome is known rather than at issuance, and why the code is CIBA §12's `transaction_failed` and never `access_denied` — the end user authenticated and denied nothing. And two tests asserted that a satisfied context yields a code while proving nothing: their stored `params.claims` was the JSON string the request helper produces, so `oidc.claims.id_token` was undefined and both ACR checks returned before comparing anything; re-anchored, then shown to have teeth by reverting the getter (2 fail without, 0 with). Also corrected [[totp-second-factor]], whose "what deliberately did not change" section asserted that `acr` is not set and `acr_values` not honoured — a false sentence in the worst possible place, since a reader deciding whether step-up is supported would have taken it at its word. Follow-ups filed as #45 (the dead `remember`/`transient` key, the same shape one field over), #46 (`amr` never reaches an ID token because it is absent from the claims configuration) and #47 (the shared handler does not deliver errors thrown on the resume route, now worked around twice).
   graph: +1 node, +2 typed edges

## [2026-09-13] ingest | [[settings-console-descriptor]], [[feature-flag-gating]] — a change made a saved setting take effect without a restart, and the interesting part is how little had to change: the console warned about a restart for all 63 catalogued settings while exactly one statement was responsible, `Object.assign(ApplicationConfig, await configStore.get())` at module load. An audit of every key found none read into module-scope state — flags are read per request by the gate and by CORS, both metadata documents are computed per request, the derived view is mutated in place — so the feature is one assignment plus a vocabulary: `apply: 'restart'` with a written `restartReason` on the descriptor, absent meaning applied on save. Four things the diff cannot carry. The two settings expected to stay restart-bound both had mechanical answers (`quick-lru`'s `resize` keeps the per-origin counters an operator must not be able to forgive; dropping the Sentry arming latch re-points the client), so the shipped classification is 63 applied-on-save and none restart-bound, and the console's notice became a signal about drift rather than a standing fact. Applying a SUBSET can produce an in-force configuration the boot validator would refuse — the Sentry card sends "enable" and "credential" together and reporting requires a credential — so `applySettings` re-validates the candidate it is about to assign and withholds the whole apply otherwise. Invalidation had to be a registry (`onSettingsApplied`) rather than imports, because the state derived from settings lives under models/, plugins/ and sentry/, all of which already import the configuration layer. And the audit trail deliberately did NOT gain the applied/waiting split: with no restart-bound setting the two lists are the same list, and a second copy of it in append-only storage answers no question. Also fixed a standing defect the work exposed: the Sentry card asserted "applies immediately" unconditionally while its own endpoint computed `restartRequired`, and neither reconciled with the other.
- 2026-09-14 — spec 047: corrected [[unknown-request-parameters]] — the ignore rule reaches inside a parameter's value; the `claims` object closed it and the refusal misreported the cause.
- 2026-09-14 — spec 049: added [[remember-me-session-retention]] — the "Remember me" checkbox never did anything, and the reason is four independent defects on one path, each sufficient alone: the resume step read `remember` while every producer writes `transient`; both password producers wrote the polarity backwards, so fixing only the key would have shipped the control in reverse; `session.payload.transient` had no reader at all, so no session this server ever issued was scoped to the browsing session; and `loginAccount` set the flag without ever clearing it, so a re-sign-in asking to be remembered kept the earlier decline. Only the first was in issue #45; the fourth was found while planning and would have survived fixing the other three. Three things the diff cannot carry. The name was settled by counting — `transient` in eight places including both persisted schemas and `loginAccount`'s own parameter, `remember` in exactly one — and accepting both was refused under Constitution VII because nothing can depend on a key that never had an effect. Omitting `expires` is sufficient only because Elysia's `Cookie.set()` resets from the route schema's initial attributes rather than merging onto the cookie's current state; verified in `node_modules`, and had it merged the same edit would have compiled and silently done nothing. And the suite actively concealed this: two cases carried the distinction in their names while asserting only that a redirect completed — the [[cookie-path-scoping]] blindness one field over — so they were re-anchored to the response `Set-Cookie` rather than deleted. Seven cases added across three files, no existing case changed, because nothing in the suite asserted `Expires` on `_session`.

- 2026-09-15 — corrected [[end-user-cookie-attributes]], [[upstream-federation]], [[security-assurance-evidence]]: `endUserCookieAttributes.sameSite` moved from `strict` to `lax`. `strict` withholds a cookie on a cross-site-INITIATED top-level navigation, which is the shape of every sign-in that begins at a relying party — the RP navigates to /auth, which sets `_interaction` and redirects to /ui/${uid}/login, whose guard requires it — so the guard answered 422 `Invalid interaction cookie` for every real client while a manual reload of the identical URL returned 200, because a browser-initiated navigation counts as same-site. `_session` shared the constant and the same fault, with a quieter symptom: invisible at /auth, so a second RP re-prompted a user who was already signed in. Two consequences for the wiki beyond the value itself: the federation three-hop design was justified partly by `strict`, and only the cookie PATH was ever load-bearing there; and the threat model listed CSRF as resting on `SameSite=Strict`, which is now `Strict` for the console and `Lax` for the end-user screens. The admin cookie is a separate constant and unchanged.
- 2026-09-15 — ingested `bucket-is-an-issuer` (a user bucket is a tenant with its own issuer); corrected the falsified claim in `admin-console-signin` that the provider session is global to the browser, narrowed the bucket-resolution section of `account-resolution`, and repaired the stale `resolveBucketForClient` name across three pages.
- 2026-09-16 — corrected [[bucket-is-an-issuer]] at `bad9975`: the asymmetry is not one branch on the default bucket but one predicate, `isServedAtTheRoot`, over two reserved buckets. The page already said both are served at the root and unaddressable, and `isAddressable` already excluded both — but `issuerFor` tested `bucket._id === DEFAULT_BUCKET_ID`, so the administrators bucket was reachable at no prefixed address and yet minted tokens claiming `<ISSUER>/admin`, an identifier no metadata advertises. Every agent connection to the admin MCP plane authorized successfully and was then refused, which is indistinguishable from an authorization failure and is not one: the flow completes and the token is what is wrong. The defect was not a missing case but the same rule written twice, once per side, with only one side updated — so `isAddressable` now derives from the predicate rather than holding a second list of reserved ids.
- 2026-09-16 — corrected [[bucket-is-an-issuer]] again, same day and same rule: the console was the third copy. Its Buckets table rendered the slug whenever a bucket had one, so the default bucket — which holds the slug `default` because a session cookie must be named after something — was listed at `/default`, verified 404 on the live instance. The Address column is the one field an operator copies when pointing a client at a bucket. `isServedAtTheRoot` moved down to the import-free `lib/admin/consts.ts` so the browser bundle can reach it, and the console's decision was extracted to `bucketAddressFor` — a render function cannot be checked against the router that has to agree with it. Three states, not two: served at the root, a prefix, and no address at all, the last being a pre-slug bucket rather than a synonym for the first.
- 2026-09-16 — corrected [[bucket-is-an-issuer]] and [[cookie-path-scoping]]: the session cookie is now `_session_<bucket>`, so a browser holds one sign-in per bucket. The feature had shipped with isolation but not co-existence — reaching a second bucket correctly asked you to sign in, and that sign-in then overwrote the first, signing the end user out of an application they had not touched. Found by probing rather than by a test: nothing in the suite compared a bucket's sign-in before and after another bucket's. The name carries the partition and the path cannot, because the default bucket is served at the root and its cookie must live at `Path=/`. `sessionCookieName` is the one derivation, in the import-free `lib/consts/param_list.ts` because the writer (`shared/session.ts`) already imports the reader (`models/session.ts`). A slug-less bucket falls back to the default bucket's name, not its own record id — the id made the sign-in screen, which knows the bucket from the client, disagree with `/auth`, which knows only the address, so a completed sign-in did not exist on the next request. The bare `_session` is legacy: never read, actively expired.

- 2026-09-16 - corrected [[deletion-and-revocation]] at `f0ea7db`: container deletion is no longer "refused while occupied". A project may take its clients and a bucket its end-user accounts, on an election carrying what the caller reviewed, so a client or account that arrived since is refused rather than destroyed on the strength of a stale consent. A project still never reaches the bucket it pointed at, under any election - buckets are shared and hold people no project knows about. One deletion now writes one audit entry carrying kind to count, replacing one entry per withdrawn declaration; the argument for those ("a bare number would not say which audiences stopped being served") does not survive an all-or-nothing cascade, where the container identity already determines which ones went. Also closes a live gap the page never mentioned because nothing had found it: the bucket delete route does not go through `loadBucketForEdit`, so it reached no reserved-bucket guard, and an empty administrators or default bucket was deletable by a super administrator.
- 2026-09-17 — corrected [[upstream-federation]] and [[interaction-page-families]] after connecting Google as a recognised provider: a recognised-provider catalogue (`lib/consts/known_providers.ts`, Google its only entry) reduces a connection to two pasted values, and `FederationProvider` gains no field for it — so nothing downstream is able to branch on how a provider was configured, and a hand-configured Google provider is recognised on equal terms with no migration. Two findings recorded as gotchas. A slugless bucket's callback address is built from its record id and is silently invalidated by assigning a slug, surfacing weeks later as `redirect_uri_mismatch` on the upstream's page with no trace here; it is reported rather than refused. And the login POST's refusal path rendered from `loginServer`'s defaults, so every mistyped password removed the bucket's provider buttons from a page that had just shown them — the third distinct way that page has lost content it rendered, and the first where the props were not to blame.
- 2026-09-17 — added [[recognised-provider-divergences]] and corrected [[upstream-federation]] after adding Apple, Microsoft and GitHub: the catalogue holds four entries, and the catalogue's closing assumption that the rest would be "a matter of adding data" was false for all three. Microsoft's issuer contains the organisation and its multi-organisation metadata states that issuer as a literal `{tenantid}` placeholder, so matching became a rule and the §4.3 equality check is compared against the pattern for a templated entry only — with `tid` doing the actual restricting, since any organisation's assertion otherwise verifies correctly. Apple issues no secret but a six-month-lived assertion derived from a stored key, so it is minted per exchange, and its posting-back return is mandatory rather than optional. GitHub asserts no identity at all and is read back through its API behind a new seam at `(subject, claims)` — `resolve.ts` is untouched by the whole feature. The finding worth carrying: PKCE support was inferred from published metadata, and three of the four providers support the binding while only one advertises a method, so three legs were silently getting none — a weakness in what the first catalogue entry shipped rather than a cost of this change. Apple's own support is unresolved and its entry says so.
- 2026-09-18 — corrected the SEO rule count on [[security-assurance-evidence]] from twenty-two to twenty-three (verified by enumerating `fail()` ids in website/scripts/seo/verify.ts) while adding the blog page type; the blog added no new rule, it extended claimSurface's reach to /blog/.
- 2026-09-18 — corrected [[bucket-is-an-issuer]] after host-addressed buckets shipped: a bucket is now addressed by a path segment **or** a hostname of its own, never both, and the three-way answer moved into one derived `addressOf` in `lib/configs/issuer.ts` that `issuerFor`, `pathPrefixFor` and `sessionCookieName` all switch on. Four findings the page now records. `addressOf` needs a fourth `unaddressed` state because those consumers disagree on purpose — the issuer falls back to the record id, the cookie to `default`, and the comment in `param_list.ts` already said why collapsing them writes "a cookie nobody reads". A host-addressed bucket needs no cookie-name suffix at all, since the suffix exists only because path-addressed buckets share one origin. The host is read from `Host` and never `X-Forwarded-Host`, which is attacker-settable and here selects the tenant. And `/ui/*` is served from one origin for every path-addressed bucket, so a hostname made the origin a real boundary for the first time: an interaction now records the bucket it began at, because without it an interaction begun at one bucket's host could be completed at another's — written as a verification point in the plan rather than assumed, and it turned out to be a live gap. Also corrected AGENTS.md, which claimed the suite uses Chai assertions; Chai is not a dependency and no spec imports it, so a chained `expect(x).to.equal(y)` fails at runtime.
- 2026-09-21 — corrected [[bucket-is-an-issuer]]: `pathPrefixFor` is gone. A Codecov patch report on 536d9b7 showed its fifteen lines never executed, and the reason was not a missing test — the function had no caller anywhere in `lib/`, `database/` or `test/`. Routing never needed it, because `lib/index.ts` mounts the same plugins under a dynamic `/:bucket` segment rather than computing a prefix; the console needs the string and derives its own in `lib/admin/ui/bucketAddress.ts`, which cannot delegate to `lib/configs/issuer.ts` because it is bundled for the browser and that module reads `ISSUER` from the environment. The same report surfaced a second unreached path that WAS a defect: `UniqueValueTaken` is raised by all three bucket stores and was caught by nobody, so two operators assigning one hostname at the same moment got a 500 and a recorded fault instead of the 409 the class was introduced to produce. Both halves are now proved by cases on the create and the address-change routes.
- 2026-09-21 — corrected [[cookie-path-scoping]] and [[bucket-is-an-issuer]]: "the bucket comes from the address" was a claim the code no longer earns, and it named a live defect. Two buckets are served at the root — the default one and the administrators' — so the bare paths are one address for two populations, while `resolveBucketForRequest` has always answered the same question from the client for the resumption, the login POST and `findAccount`. Naming the session cookie from the address made `/auth` read `_session_default` and record that session on the interaction, while `resume()` read `_session_admin` and found another — `interaction session and authentication session mismatch`, but only for an operator whose browser already held an authenticated default-bucket cookie, since an interaction records a session only when it carries an account. That condition is why a clean browser signed in perfectly and no spec could see it, and the same gap also meant a console session was never recognised at `/auth`, so the password was asked for on every authorization request. Fixed with one field and one writer: `OIDCContext.signInBucket`, defaulting to the address and assigned from `checkBucket`'s own resolution — which is why that check now returns the bucket it compared against, and why the session is read after it rather than at the top of the handler. Not folded into `oidc.bucket`, which `issuerFor` stamps into tokens. Signing out had the same hole and could not take the same fix — `POST /logout/confirm` carries no client identifier and finds the session by cookie name, so resolving the first leg from the client would only move the disagreement to the second. It refuses instead: a client that does not share the addressed cookie may not end that sign-in, compared by cookie name so that buckets sharing one keep ending each other's. Found while closing the sign-in half, and reachable with an ordinary named bucket rather than only the console. The fixture gap underneath it is worth carrying too: `seedBucket`/`clearSeededBuckets` never called `forgetBucketAddresses`, so two spec files seeding one bucket id with and without a slug left the second reading the first's address — a failure that depends on the order Bun walks a directory in, which differs by OS.
- 2026-09-22 — corrected [[feature-flag-gating]]: the flat-key decision is not only about `ApplicationConfig`. A **client's registration data is a flat list on purpose too**, and it was written down nowhere, so a proposal to nest the fields that are validated together — redirect targets with the application type, grant types with response types — read as a reasonable design rather than as something already settled. Ruled out on the page with the reason: grouping relocates a rule instead of removing a state, nothing becomes impossible, around 68 call sites change, and the object stops matching the shape the rest of the configuration surface uses. The admissible move is the opposite one — several flat fields that were genuinely alternatives collapsing into one flat field — and the rule that decides any such proposal is now written out: a flat field may replace several flat fields that were alternatives; it may not become a nested object holding fields that were never alternatives. The two candidates that fit, the five `tls_client_auth_*` certificate subject attributes and the mutually exclusive proof-of-possession pair, are recorded as admissible in principle; neither has been done, and no code changed with this entry.
- 2026-09-23 — added [[client-registration-attributes]]: registration metadata and the base registration keys are now declared once each, in `lib/consts/client_attributes.ts`, replacing five parallel lists whose failure mode was an attribute silently ignored. The page records what fails silently when editing it — declaration order is the stored key order, the two precedence lists decide which refusal a registration breaking two rules receives, and the structural layer stays in `ClientSchema` because folding it in once turned eleven refusals into acceptances — plus why the secret and key-set requirements must not be unified: they ask opposite questions over attribute sets that differ for protocol reasons.
- 2026-09-23 — moved the reasoning out of AGENTS.md, which had grown into a second wiki loaded into every session, and left one-line rules there pointing here. Added [[signing-keys]], [[admin-provisioning]] and [[end-user-onboarding]]; extended [[feature-flag-gating]] (the three configuration surfaces, `onSettingsApplied` invalidators, `apply: 'restart'`), [[addon-registry]] (the per-spec baseline), [[token-payload-access-contract]] (the shallow storage projection and `tryFind`/`find`), [[postgresql-backend]] (datastore selection, the refuse-only migration gate, the empty set), [[bucket-is-an-issuer]] (unknown hosts beneath the domain are refused, others fall through; the address-change route), [[mcp-server-authorization]] (the CIMD flag and egress bounds) and [[error-store-capture-sites]] (recording never blocks; a full queue is counted). Two claims AGENTS.md carried were stale and are corrected rather than moved: key generation is no longer RSA-only or persist-then-restart — it hot-applies in every known algorithm, and only deletion waits — and the registration door resolves through `resolveBucketForRequest`, not `resolveBucketForClient`. Also corrected [[group-ownership]], which described `lib/admin/groups/migration.ts` as live; it was retired at `92a60cd`.
- 2026-09-23 — corrected [[client-identity-from-database]]: a resolved client is now frozen data. The prototype carrying eleven forwarding methods, two aliases and a getter is gone, key material is derived beside the client by `clientKeys` in a `WeakMap`, and back-channel logout and CIBA ping moved to `lib/shared/client_notifications.ts`. Worth recording because freezing surfaced tests that had been reaching states no registration could produce — an HMAC algorithm the deployment does not support, an unconfigured ACR value, a signed-userinfo algorithm with signed userinfo switched off — by assigning to the shared client object; they now change the stored record through `changeClient`.
- 2026-09-23 — corrected [[client-identity-from-database]] again: every surface now writes a client through `registerClient`, and the sector identifier document is checked there and nowhere else, so resolving a stored client makes no outbound request. The console used to store without the check, which then ran on the next resolution as a failure for whoever used the client next. A document-described client keeps the check on resolution, because it is never stored. Also corrected [[postgresql-backend]] (the no-ttl `upsert` sites) and the evidence line in [[pairwise-identifier-salt]].
- 2026-09-23 — [[client-identity-from-database]]: the validated client is now typed as what it is — `ClientSchema`'s attributes with the always-defaulted ones required — and the request context carries it typed. Recorded because the guard that holds `ALWAYS_PRESENT` to the declaration caught a miss on its first run (`dpop_bound_access_tokens`), and because typing `oidc.client` surfaced only twelve reports: most endpoints read it through a context that is itself untyped, so that is where the remaining gap is.
- 2026-09-23 — [[client-identity-from-database]]: the client's type names its closed value sets (authentication method, CIBA delivery mode, signing algorithms) from the lists the configuration check uses, is read-only all the way down to match the freeze, and the adapter's `Client` payload is a `StoredClient`. The runtime schema was deliberately left unchanged. No typo in an existing comparison was found by it; what it did surface was the console copying the frozen client's arrays into mutable fields, and a notification reading an untyped `params`.
- 2026-09-23 — [[client-identity-from-database]]: the test harness now runs the settings invalidators when `bootstrap` swaps a spec's settings, so the client validation memo no longer carries a client validated under one spec's capabilities into the next. Found by CI: `non_conform.spec.ts` had signed-userinfo cases without `jwtUserinfo.enabled`, and passed on Windows only because `conform.spec.ts` ran first and left its client in the memo.
- 2026-09-23 — added [[refresh-token-chain-bound]]: the bound that ends a browser application's refresh-token chain at its first lifetime had been dead since `54ba556` removed the ambient request store it read, and was masked because the default grant lifetime ends the chain at the same moment. The lifetime functions no longer take a request context; the rule is decided from `iiat` and `rotations` on the token.
- 2026-09-23 — corrected [[addon-registry]] and [[event-bus]]: the `{ oidc }` wrapper inherited from the Koa request object is gone, so every extension function, policy check and request-scoped event receives the request context itself; `deviceInfo` is now actually called; the event list is complete, with one argument shape per name. Worth recording because the wrapper was hiding a double emit of `grant.revoked` on a partial sign-out, in two different shapes.
- 2026-09-23 — corrected [[token-payload-access-contract]]: the request context's entity store is now typed (`OIDCEntities`), closing the third place a bare field read compiled; recorded the two it held (`Interaction.cid`, `Interaction.deviceCode`). The context's inputs are fixed at construction, with a required bucket, so [[end-user-cookie-attributes]] and [[account-resolution]] no longer quote `ctx.oidc`.
- 2026-09-24 — corrected [[token-payload-access-contract]]: request parameters are now typed from the endpoints' own schemas (`PipelineParams`, `TokenParams`), `presence` narrows the parameters it checks, and `claims` members are `unknown` until read through `claimRequest()`. Typing them surfaced a client-triggerable `server_error` for a primitive `claims.id_token.sub`, and a pushed request's `trusted` flag that never reaches the authorization request; both are fixed on the owner's decision, as is the resumption's sign-out confirmation, which wrote its state off the session payload and never saved the session, so it always answered 400.
- 2026-09-24 — corrected [[unknown-request-parameters]] and [[elysia-lifecycle]]: the route schemas now declare `x-client-cert` (and `accept`) so the typed test client can send them; the argument against `normalize` still holds for the header a deployment's `getCertificate` override reads, which no schema can name.
- 2026-09-24 — corrected [[unknown-request-parameters]]: the authorization endpoint now refuses `registration`, and a disabled `request`/`request_uri`, with the OIDC Core §3.1.2.6 codes instead of the unregistered `not_supported`.
- 2026-09-24 — corrected [[token-payload-access-contract]]: the constructor's payload check runs against `BaseModelPayload`, not the subclass schema (a subclass `model` field is defined only after the base constructor), so "a constructed model always carries a schema-valid payload" was false; `consumed` is now declared as boolean-or-timestamp, as every adapter writes it.
- 2026-09-24 — corrected [[addon-registry]]: signatures are `Overridable<typeof import(...)>` (an override may answer synchronously), accessors with an async default are `async`, and the throwing extension seams declare their contract's return type instead of the stub's inferred `Promise<void>`.
- 2026-09-24 — corrected [[account-resolution]]: `findAccount` is typed (generic over the endpoint's parameters) and resolves an absent subject to `undefined` before any bucket lookup.
- 2026-09-24 — corrected [[admin-console-signin]] (KeyStore now holds jose's `JWK`, a type alias in jose 6.2.12) and [[refresh-token-chain-bound]] (each token kind's own `expiration` getter names its `ttl` function; no class-name dispatch).
- 2026-09-24 — added [[typebox-dual-type-resolution]]: why a TypeBox schema or error crossing to Elysia fails to type-check under `nodenext`, and why the fix is structural types rather than `paths`.
- 2026-09-24 — corrected [[token-payload-access-contract]]: two more bare reads found by typing the grants — `at.aud` in three grants (ID Token claims beside a resource-bound access token) and `token.gty` in the refresh grant.
- 2026-09-24 — corrected [[bucket-is-an-issuer]]: `issuingBucket` dropped `host`, so a host-addressed bucket was taken for an unaddressed one (cookie `_session_default`, issuer `ISSUER/<id>`), and `GET /logout` ignored the request host; both fixed.
